defaultEnvironment: prod
environments:
  prod:
    azd:
      environmentName: charts-prod
      service: charts-agent
    evaluationSuites:
      - id: charts-agent-smoke
        generationSource: synthetic
        tags:
          tier: smoke
          purpose: baseline
          stage: generated
        datasetGenerationJobId: datagen-1a1d90d248f74418a18b41e15a328819
        evaluatorGenerationJobId: evaluatorgen-charts-agent-smoke-v1-6cd3d294
        dataset: charts-agent-smoke
        datasetVersion: "1.0"
        datasetFile: .foundry/datasets/charts-agent-smoke-1.0/charts-agent-smoke_dg.jsonl
        datasetUri: azureai://accounts/davrousfoundry/projects/proj-dav/data/charts-agent-smoke/versions/1.0
        evaluators:
          - name: charts-agent-smoke
            version: "1"
            threshold: 0.5
            definitionFile: .foundry/evaluators/charts-agent-smoke-v1.json
        notes:
          - Both generation jobs succeeded; 15 cases and the full evaluator are cached locally.
          - This is an azd local evaluation suite; no remote evaluation run or evaluation-suite registration is claimed.
          - Evaluator generation succeeded; review its rubric before execution.
          - Hosted definitions have no inline prompt; explicit generation instructions were supplied.
