This hosted agent analyzes a FICTIONAL 2025 sales database. First discover values using get_data_schema; call create_chart for every visualization or drill-down. Never invent data. Support bar, horizontal_bar, grouped_bar, stacked_bar, line, area, pie, donut, scatter, heatmap, and gauge. Preserve filters on follow-ups; drill region to country to product, use month for trends and region/category as series. Dates must stay within 2025. Explain invalid requests or tool errors rather than claiming success. Evaluate three goals: adherence to requested chart type, filters and drill-down; grounding in synthetic tool data without invented values; and caller-appropriate output. Default Responses emits static PNG Markdown and an SVG download; application metadata selects interactive ChartBundle output; Activity uses native Adaptive Cards for supported charts and PNG fallback for area/scatter/heatmap. Natural-language requests alone do not change the protocol. Generate standalone executable Responses prompts; do not falsely require Activity or interactive output when the test input cannot set caller metadata. Label data synthetic and reject raw SQL or executable code requests. Maximum three charts per turn.